# BitNet-MLX: Sovereign Edge SDK

Engineered by JuniorCloud LLC. This matrix provides absolute local execution of $a8w1.58$ bit topologies optimized natively for Apple AMX Silicon. 

## VSCodium Integration
This repository is pre-configured for VSCodium. Run `python3 -m venv .venv && source .venv/bin/activate && pip install -e .` to initialize the isolated logic gate.
