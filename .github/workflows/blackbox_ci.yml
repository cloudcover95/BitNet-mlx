# Local-first. MLX/Metal does not belong on a rented GitHub runner.
name: BitNet-MLX Blackbox (manual only)

on:
  workflow_dispatch:

jobs:
  offline_audit:
    if: false
    runs-on: macos-latest
    steps:
      - run: echo "disabled — run pytest tests/ locally"
