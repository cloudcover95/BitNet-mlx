# BitnetCloud consumer

JuniorHome `packs/junior_arm_iot/quant.py` is the stdlib AbsMean fallback
when MLX is off the 45 W path.

Same map: W_q = clip(round(W / mean(|W|)), -1, 1).
5-trit pack lives in that pack, not here.
This repo stays Metal / MLX kernels.
FrameForge math_kb.py still owns knockback. CPU intent only.
